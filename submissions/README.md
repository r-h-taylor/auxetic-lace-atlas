# User submissions

Drop your contributed graph topology JSONs here in PRs. The CI
(`validate_submissions.yml`) will validate, compute mechanics, and
rank each one against the deployed atlas, posting the report as a PR
comment.

See [`../contrib/README.md`](../contrib/README.md) for the submission
schema and full workflow.

Sample submissions live in [`../submission_examples/`](../submission_examples/).
